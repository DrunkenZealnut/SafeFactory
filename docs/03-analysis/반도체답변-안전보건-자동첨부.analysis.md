# 반도체답변-안전보건-자동첨부 Analysis Report

> **Analysis Type**: Gap Analysis (Design vs Implementation)
>
> **Project**: SafeFactory
> **Analyst**: gap-detector
> **Date**: 2026-03-13
> **Design Doc**: [반도체답변-안전보건-자동첨부.design.md](../02-design/features/반도체답변-안전보건-자동첨부.design.md)

---

## 1. Analysis Overview

### 1.1 Analysis Purpose

Design Section 4 구현 순서 체크리스트 8개 항목 + 추가 설계 사양 11개 항목 = 총 19개 검증 항목에 대해 설계 문서와 실제 구현 코드의 일치도를 검증한다.

### 1.2 Analysis Scope

| Category | Path |
|----------|------|
| Design Document | `docs/02-design/features/반도체답변-안전보건-자동첨부.design.md` |
| Implementation 1 | `services/rag_pipeline.py` |
| Implementation 2 | `api/v1/search.py` |
| Implementation 3 | `services/domain_config.py` |

---

## 2. Design Checklist Verification (8 Items)

| # | Checklist Item | File | Status | Evidence |
|---|----------------|------|:------:|----------|
| 1 | `_search_safety_context()` 함수 작성 | `services/rag_pipeline.py` | ✅ | L213-262: 함수 존재, docstring/signature/동작 모두 설계 일치 |
| 2 | `run_rag_pipeline()`에 Phase 7.5 추가 | `services/rag_pipeline.py` | ✅ | L678-688: Phase 7.5 블록 존재, 조건/로깅/result 할당 모두 일치 |
| 3 | `build_llm_prompts()`에 `safety_references` 파라미터 + 주입 로직 | `services/rag_pipeline.py` | ✅ | L721-795: 파라미터 추가, 주입 로직 설계와 동일 |
| 4 | `build_llm_messages()`에 `safety_references` 전달 | `services/rag_pipeline.py` | ✅ | L824-835: 시그니처에 `safety_references=None` 추가, `build_llm_prompts()`에 전달 |
| 5 | `api_ask()`에서 `safety_references` 전달 | `api/v1/search.py` | ✅ | L313-317: `pipeline.get('safety_references')` 추출 후 `build_llm_messages()` 전달 |
| 6 | `api_ask_stream()`에서 `safety_references` 전달 | `api/v1/search.py` | ✅ | L428-432: `pipeline.get('safety_references')` 추출 후 `build_llm_messages()` 전달 |
| 7 | `DEFAULT_SYSTEM_PROMPT`에 안전보건 통합 지침 추가 | `services/domain_config.py` | ✅ | L228: 11번 항목으로 안전보건 정보 통합 지침 추가됨 |
| 8 | `DOMAIN_COT_INSTRUCTIONS["semiconductor"]`에 안전 검토 단계 추가 | `services/domain_config.py` | ✅ | L411: 6번째 단계 "안전보건 검토" 추가됨 |

**Checklist Result: 8/8 (100%)**

---

## 3. Additional Design Specs Verification (11 Items)

### 3.1 Constants

| Constant | Design Value | Implementation Value | Status |
|----------|:------------:|:--------------------:|:------:|
| `SAFETY_CROSS_SEARCH_TOP_K` | 5 | 5 (L206) | ✅ |
| `SAFETY_CROSS_SEARCH_MIN_SCORE` | 0.3 | 0.3 (L207) | ✅ |
| `SAFETY_CROSS_SEARCH_MAX_RESULTS` | 3 | 3 (L208) | ✅ |
| `SAFETY_CROSS_SEARCH_MAX_CHARS` | 1500 | 1500 (L209) | ✅ |
| `SAFETY_CROSS_SEARCH_NAMESPACE` | `'kosha'` | `'kosha'` (L210) | ✅ |

### 3.2 Function Behavior

| Spec | Design | Implementation | Status |
|------|--------|----------------|:------:|
| Search method | `get_agent().search()` with kosha namespace | L224-229: `agent = get_agent()` then `agent.search(query=query, top_k=..., namespace=...)` | ✅ |
| Score filtering | `>= 0.3` | L232: `r.get('score', 0) >= SAFETY_CROSS_SEARCH_MIN_SCORE` | ✅ |
| Results limit | top 3 | L238: `results[:SAFETY_CROSS_SEARCH_MAX_RESULTS]` | ✅ |
| Total chars limit | 1500 | L249-253: `remaining = SAFETY_CROSS_SEARCH_MAX_CHARS - total_chars` with truncation | ✅ |
| Graceful degradation | `try/except` + `logging.warning` + return `''` | L223,260-262: `try/except Exception` + `logging.warning("[SafetyCross] Cross-search failed: %s", e)` + `return ''` | ✅ |
| Citation format | `[S1]`, `[S2]` (not `[1]`, `[2]`) | L255: `f"[S{i+1}] (출처: {source_file})\n{content}"` | ✅ |

### 3.3 Integration Points

| Spec | Design | Implementation | Status |
|------|--------|----------------|:------:|
| Phase 7.5 trigger condition | `namespace in ('', 'semiconductor-v2')` | L681: `if namespace in ('', 'semiconductor-v2'):` | ✅ |
| Prompt injection text | `"⚠️ 관련 안전보건 정보"` instruction | L793: `답변 말미에 "⚠️ 관련 안전보건 정보" 섹션을 추가하세요.` | ✅ |
| Fallback instruction | `"관련성이 낮으면 이 섹션을 생략하세요"` | L795: `관련성이 낮으면 이 섹션을 생략하세요.` | ✅ |
| `build_llm_messages` passthrough | `safety_references` parameter forwarded to `build_llm_prompts` | L828-831: parameter received and passed through | ✅ |
| `api_ask` extraction | `pipeline.get('safety_references')` passed to `build_llm_messages` | L313: `safety_refs = pipeline.get('safety_references')` then L317: passed as arg | ✅ |
| `api_ask_stream` extraction | `pipeline.get('safety_references')` passed to `build_llm_messages` | L428: `safety_refs = pipeline.get('safety_references')` then L432: passed as arg | ✅ |

**Additional Specs Result: 11/11 (100%)**

---

## 4. Text-Level Comparison

Design 문서의 코드 블록과 실제 구현의 텍스트 일치도를 줄 단위로 검증한다.

### 4.1 `_search_safety_context()` 함수 본문

| Design Spec | Implementation | Match |
|-------------|----------------|:-----:|
| Docstring: "Cross-search kosha namespace..." | L214: 동일 | ✅ |
| Args: query description | L217: "Search query (after mention parsing)." | ✅ |
| Returns: "Formatted safety references string..." | L220: 동일 | ✅ |
| Format: `[S{i}] (출처: {source_file})\n{content}` | L255: `f"[S{i+1}] (출처: {source_file})\n{content}"` | ✅ |
| Empty return on no results | L234-235: `if not results: return ''` | ✅ |
| `'\n\n'.join(parts)` | L258: `return '\n\n'.join(parts)` | ✅ |

### 4.2 Phase 7.5 블록

| Design Spec | Implementation | Match |
|-------------|----------------|:-----:|
| Comment: `# Phase 7.5: Safety Cross-Search (semiconductor only)` | L679: 동일 | ✅ |
| `safety_refs = _search_safety_context(search_query)` | L683: 동일 | ✅ |
| `result['safety_references'] = safety_refs` | L685: 동일 | ✅ |
| Log: `[SafetyCross] Found safety context (%d chars)` | L686: 동일 | ✅ |
| Outer `try/except` with `[SafetyCross] Failed: %s` | L687-688: 동일 | ✅ |

### 4.3 Prompt Injection Block

| Design Spec | Implementation | Match |
|-------------|----------------|:-----:|
| Section header: `## 관련 안전보건 참고자료` | L790: 동일 | ✅ |
| Instruction text (4 lines) | L793-795: 글자 하나까지 동일 | ✅ |
| `if safety_references:` guard | L787: 동일 | ✅ |

### 4.4 `domain_config.py` Additions

| Design Spec | Implementation | Match |
|-------------|----------------|:-----:|
| DEFAULT_SYSTEM_PROMPT 11번 항목 텍스트 | L228: 설계 문서와 동일한 문구 | ✅ |
| DOMAIN_COT_INSTRUCTIONS["semiconductor"] 6번째 단계 텍스트 | L411: 설계 문서와 동일한 문구 | ✅ |

---

## 5. Non-Goals Verification

설계 문서 Section 6 비목표 항목이 올바르게 구현되지 않았는지(= 비목표를 지키고 있는지) 확인한다.

| Non-Goal | Verification | Status |
|----------|-------------|:------:|
| 안전보건 결과를 `sources` 배열에 합치지 않음 | `run_rag_pipeline()`에서 `result['safety_references']`는 별도 키로 저장, sources 배열 미수정 | ✅ |
| `safety_references`를 SSE `metadata` 이벤트에 포함하지 않음 | `api_ask_stream()`의 `meta_data` dict에 `safety_references` 미포함 | ✅ |
| 다른 도메인에 크로스 검색 적용하지 않음 | Phase 7.5 조건: `namespace in ('', 'semiconductor-v2')` 로 제한 | ✅ |
| 병렬 검색 최적화 적용하지 않음 | `_search_safety_context()`는 Phase 7.5에서 순차 호출 | ✅ |

---

## 6. Match Rate Summary

```
+---------------------------------------------+
|  Overall Match Rate: 100%                    |
+---------------------------------------------+
|  Checklist Items:     8/8   (100%)           |
|  Additional Specs:   11/11  (100%)           |
|  Text-Level Match:   17/17  (100%)           |
|  Non-Goals Respected: 4/4   (100%)           |
+---------------------------------------------+
|  Total Items Checked: 40                     |
|  Matched:             40                     |
|  Gaps:                 0                     |
|  Added (not in design): 0                    |
+---------------------------------------------+
```

---

## 7. Overall Assessment

Match Rate **100%**. 40개 검증 항목 전수 통과.

설계 문서(Section 2~6)에 명시된 모든 사양이 구현에 정확히 반영되었다:

- 5개 상수 값 일치
- `_search_safety_context()` 함수의 검색/필터/포맷/에러처리 로직 완전 일치
- `run_rag_pipeline()` Phase 7.5 블록 코드 일치
- `build_llm_prompts()` 안전보건 주입 블록 텍스트 일치
- `build_llm_messages()` 파라미터 전달 구조 일치
- `api_ask()`, `api_ask_stream()` 양쪽 모두 `pipeline.get('safety_references')` 추출 및 전달 일치
- `DEFAULT_SYSTEM_PROMPT` 11번 항목, `DOMAIN_COT_INSTRUCTIONS["semiconductor"]` 6번째 단계 텍스트 일치
- 4개 비목표 항목 모두 올바르게 미구현

차이점, 누락, 추가 항목 없음. 설계 문서 업데이트 불필요.

---

## Version History

| Version | Date | Changes | Author |
|---------|------|---------|--------|
| 1.0 | 2026-03-13 | Initial gap analysis | gap-detector |
