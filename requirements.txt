# Pinecone Agent Dependencies
pinecone>=3.0.0,<6.0.0
openai>=1.0.0
tiktoken>=0.5.0
python-dotenv>=1.0.0
Pillow>=10.0.0
tqdm>=4.65.0
langchain>=0.1.0
langchain-text-splitters>=0.0.1

# Web Server Dependencies
flask>=3.0.0
flask-wtf>=1.2.0
flask-cors>=6.0.0
flask-limiter>=4.0.0
gunicorn>=21.0.0

# Database Dependencies
flask-sqlalchemy>=3.1.0

# Authentication Dependencies
flask-login>=0.6.0
authlib>=1.3.0
cryptography>=42.0.0

# Google Gemini (RAG answer generation)
google-genai>=1.0.0

# Anthropic Claude (RAG answer generation)
anthropic>=0.39.0

# RAG Enhancement Dependencies
rank-bm25>=0.2.2

# Optional: Local cross-encoder reranking (not needed if using Pinecone Inference API)
# sentence-transformers>=2.2.0
